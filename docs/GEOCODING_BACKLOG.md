# Geocoding backlog

Destinations outside the verified coordinate set should be geocoded against a second reliable map source before insertion via Prisma Studio. See [`ADDING_REAL_DATA.md`](ADDING_REAL_DATA.md).

## Already in many local databases (verified coordinates)

Southern gateway and upper Kaghan additions were historically added as real rows (not via a current seed script). Confirm coordinates before expanding further.

## Still to geocode

All remaining valleys and stops from Build.md's extended network (Ishkoman, Broghil, Kalam expanded, Kumrat expanded, Neelum expanded, AJK southern belt, etc.) must be geocoded before insertion. Do not add placeholder coordinates.
